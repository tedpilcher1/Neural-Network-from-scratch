use crate::forward_propagation::apply_forward_propagation;


fn apply_gradient_descent () {

    // forward propagation
    // let mut fpv = apply_forward_propagation();
    // backpropagation

    // add regularisation terms

    // update params 

    // optionally print the loss
}